# Trading journal 2026-08-31

## Spec draft — equities day trading playbook (not implemented)

- Wrote `playbooks/equities_day_trading.md` as **DRAFT**. Awaiting owner approval. No pipeline, Agent H, or Automation changes.
- Copied existing equity limits: SL **−20%** of cost, TP **+25%** of cost, size up to full buying power, max one open position, ADV ≥ 2,000,000, RTH only, Agentic ••••2907.
- **No shorting.** Long shares only. Bearish names skip this playbook.
- Day-trade extras in the draft (for owner to accept or strike): flatten before 16:00 ET; no new entries after 15:45 ET.
- This chat did **not** place. Agent H still follows the options playbook + existing equity fallback only.

## Spec lock — equities playbook RELEASED (owner 2026-08-31)

- Owner said **Released**. `playbooks/equities_day_trading.md` is RELEASED.
- Day-trade extras kept: flatten before 16:00 ET; no new entries after 15:45 ET.
- Live `place_*` from this chat still needs an explicit confirm of a **specific** order. This release is not a blanket authorize-all.
- Wired as options-first equity path in the signal pipeline and Agent H prompt. **No orders placed** from this chat (outside RTH at release).

## Main lock files for Agent H (2026-08-31)

- `main` previously had only `README.md`, so H’s Sunday fire could not read permissions or rules.
- Trading branch is being landed on `main` so the next H checkout can see `config/rules.json`, `config/autonomous_permissions.json`, and the playbooks.
- Outside RTH H still must not call Robinhood. It should journal `lock_files: present` or `missing` before exit.
- Do **not** merge H’s draft PR #3 over this journal. **No orders placed.**

## Audit — errors, duplicates, inconsistencies (2026-08-31)

- One source of truth: `config/rules.json`. Permissions file is kill switch + tool allowlist only.
- Agent F is blocked during RTH while H is enabled. Do not place from `signals/*` snapshots (banners added).
- Working-order filter uses MCP `state` values (no `open` flag). Intraday bars are `10minute` only.
- Options may hold overnight with the broker stop; equities still flatten before 16:00 ET. Loop JSON no longer says flatten for both.
- README + `AGENTS.md` added. Stale “H not activated / overnight loop / 15m bars” docs updated.
- Skip journals append on `main`. Do not open a new PR per outside-RTH skip.
- **No orders placed.** Re-paste the H prompt after this lands; git does not update the stored Automation text.

## Spec lock — options overnight hold (owner 2026-08-31)

- Owner said **keep current lock**. Open long options **may be held overnight** with the broker **−20%** stop. Do **not** flatten options at 16:00 ET.
- Still: no new option entries overnight or after **15:45 ET**. Equities still flatten before close.
- `config/rules.json`: `may_hold_overnight_with_stop: true`, `flatten_at_close: false`, `overnight_lock_confirmed: 2026-08-31`.
- **No orders placed.**

