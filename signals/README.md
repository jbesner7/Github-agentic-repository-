# Historical signal snapshots

Files in this directory are **read-only snapshots** from the 2026-08-30 weekend cycle (and later dry reviews). Quotes are stale. Some rows still show DRAFT playbook status or superseded risk math (−7% / +20%).

**Do not `place_*` from these files.** Live tickets must be re-quoted in RTH against `config/rules.json`.

Pipeline writes may overwrite JSON on a later cycle. Treat anything with `"historical": true` or `"do_not_place": true` as dead.
